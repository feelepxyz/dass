[project]
name = "dass-cad"
version = "0.1.0"
description = "Parametric CAD model and cut list for the outdoor toilet"
requires-python = ">=3.11"
dependencies = [
    "cadquery>=2.5,<3",
    "numpy>=2,<3",
    "pillow>=11,<13",
]

[tool.uv]
package = false
